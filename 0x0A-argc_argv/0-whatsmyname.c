#include "main.h"
#include <stdio.h>
/**
*main - is entry point
*@argc: is an argument
*@argv: is an argument too
*Return: Always 0
*/
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
