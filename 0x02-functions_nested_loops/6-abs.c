#include "main.h"
/**
*_abs - is a a function for boult value
*@n: is a argument
*Description:is a a programm for boult value
*Return: Always 0
*/
int _abs(int n)
{
if (n < 0)
return (n * -1);
else
return (n);
}
